problem1 ← ⊢(={⍺/⍵}⊢)⌈\
problem2 ← {(1-≢⍺)⌽⍺⍷⍵}
problem3 ← {a←' ',⎕A⋄(⍺⌽a)[a⍸⍵]}
problem4 ← ⊃0~⍨>-<
problem5 ← { s ← ⍺⌊⍥≢⍵ ⋄ +/¨ 0 1 ∘.= ⊂⍺>⍥(s∘↑)⍵ }
problem6 ← ((2,⍨(2÷⍨≢)) ⍴ ⊢) (3=⍳)⊆⊢
problem7 ← 1∘∧,÷∘(1∘∨)
problem8 ← (3↑∘⊃¯1⎕DT ⊢ + (⌊/ 644 759 924 21252 | -))⍥(1 ⎕DT⊂)
problem9 ← {↑(⊃∘(,/ 1⌽¨ {2|⍳≢⍵} ⊂ ⊢)¨,/) ⊂¨⍵}
problem10 ← {⍺↑ (⍺-1) (↑{({⍵/⍨' '≠⍵}¨⍺) , (,/⍵)}↓) (1,' '=⍵)⊂⍵}
